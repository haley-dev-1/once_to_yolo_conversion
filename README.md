# ONCE 

## Goals
Convert ONCE annotations to YOLO format

Put them in a new file per image

## YOLO Format
<class_id center_x center_y width height>

## TODO: 
make cameras accessible, probably via regex
i need to make sure each frame is a file.